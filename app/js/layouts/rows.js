(function ($) {
    PhotoMosaic.Layouts.rows = function (images, opts) {
        this.images = images;
        this.opts = opts;
        return this.init();
    };

    PhotoMosaic.Layouts.rows.prototype = {
        init : function () {
            // 
        }
    };
}(window.JQPM));